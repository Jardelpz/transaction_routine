services:
  postgres:
    image: postgres:16
    container_name: transaction_routine_db
    environment:
      POSTGRES_PASSWORD: "pg123"
      POSTGRES_USER: "postgres"
      POSTGRES_DB: "transaction_routine_db"
    ports:
      - "5432:5432"
    volumes:
      - ./scripts:/docker-entrypoint-initdb.d
